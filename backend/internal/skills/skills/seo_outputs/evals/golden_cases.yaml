cases:
    - forbidden_qualities:
        - fabricated_metrics
        - unapproved_side_effect
        - raw_secret
        - internal_identifier_disclosure
      id: golden_contract_1
      input: Use the approved context to package SEO recommendations into explicit reviewable outputs.
      required_outputs:
        - seo_outputs_draft
        - quality_summary
      required_qualities:
        - intent_aligned
        - evidence_bounded
        - safe_summary
        - no_protected_metadata
skill_id: seo_outputs
version: 1
